# Task 23 — Full backtest and ablation suite (results memo)

This is a results memo, not a success story. Numbers below are from the
production stack as wired in Task 22B, walk-forward 2019–2025 (2020 continuity
only), archived under `data/backtests/task23_*` and `file:./mlruns`.

Acceptance driver: `scripts/_task23_backtest.py` (same pattern as Tasks 13–15
acceptance scripts). No new production subsystems.

---

## Executive verdict

1. **A2 (ratings frozen after Week 1) is materially worse than the full
   system.** MAE +1.83 pts, ATS −2.3 pp, CRPS +1.43. The continual-learning
   claim is **not** falsified by this run — freezing Stage-1 updates hurts.
2. **§1.6 success criteria are mostly missed** on the measurable slice. State
   that plainly; do not tune.
3. **Market / bet-layer measurement for 2021–2025 is blocked** without Odds
   historical snapshots (estimate 54,090 credits vs 16,000 ceiling). ATS/OU vs
   close and CLV below are **2019 CFBD-regime only** (n_ATS = 865), never
   pooled with null-line seasons.

---

## Data and prior-task blockers (do not silently patch)

| Blocker | Effect on Task 23 |
|---|---|
| `ReturningProductionSchema` / roster `athlete_id` reject CFBD negatives | Could not ingest `returning` / `roster`; fitted priors fall back to `initial_team_state` (A1 ≈ full on priors) |
| No Odds historical snapshots (Task 5B STOP awaiting spend) | Seasons ≥2021: all `spread_*` / `total_*` null (§7.2 item 8 ladder excludes CFBD). Estimate: 54,090 credits > ceiling 16,000 |
| `production_stack` emits point preds only (no σ / probs / MC) | Prob metrics use fixed σ_m=14, σ_t=13.5 Normal post-hoc |
| Garbage-time WP absent on staged plays | A5 play counts identical on/off; A5 is a **silent no-op** |
| CLI `backtest run` does not load `priors_frame` or pre-test seed labels | Script loads 2014–2025 observations for rating continuity; mapping layer still cold-starts into 2019 then accumulates |

Ingest performed for this task (minimal endpoints `games,plays,advanced,lines,teams`):
2014–2025 staged. Returning/roster/talent/recruiting skipped after schema failures.

---

## Headline metrics — fundamental stack

Finite-row recomputation (one missing realized margin made suite CRPS NaN;
corrected values below). ATS/OU only where `spread_close` / `total_close` exist
→ **2019 only**.

| Metric | Value | Notes |
|---|---|---|
| n_predictions | 6003 | 2019–2025 incl. continuity rows |
| n_headline | 5435 | 2020 excluded |
| MAE margin | **14.196** | |
| CRPS margin (model) | **10.282** | σ=14 |
| CRPS margin (market = N(−close, 14)) | **8.613** | model **worse** than close |
| ATS vs close | **49.36%** [47.4%, 51.8%] | n=865; CI from block bootstrap on suite |
| OU vs close | **52.82%** | n≈865 |
| Log-loss ATS (model / market@0.5) | 1.046 / 0.693 | model worse |
| Calibration ATS slope | **−0.005** | far outside [0.9, 1.1] |
| Week-4 MAE | 15.013 | |
| Week-10 MAE | 13.654 | **Week 10 < Week 4 ✓** |

Market-aware stack (same seasons): MAE 15.52, ATS 47.5%, CRPS 11.33 — **worse**
than fundamental. Without snapshots, “market features” are mostly empty/null
indicators; A3 (market off) reproduces fundamental exactly.

---

## A2 — the headline ablation

| | Full (continual) | A2 (frozen after W1) | Δ (A2 − full) |
|---|---:|---:|---:|
| MAE margin | 14.196 | 16.030 | **+1.834** |
| CRPS margin | 10.282 | 11.713 | **+1.431** |
| ATS accuracy | 0.4936 | 0.4705 | **−0.0231** |

A2 scope (Task 22B): freezes **Stage-1 rating state** only; efficiency features
and mapping retrains continue. This is a **lower bound** on total in-season
learning gain. Even so, freezing ratings is clearly worse — core thesis
directionally supported.

MLflow: `745e628d5ccb4150885d38d275eb283d` (`task23_a2:A2_frozen_after_week_1`).

---

## Full ablation table (vs fundamental unless noted)

| Run | Δ MAE | Δ ATS | Δ CRPS | Comment |
|---|---:|---:|---:|---|
| A1 league-mean priors | +0.045 | 0.000 | +0.021 | Near no-op — fitted priors lacked returning/talent |
| **A2 frozen ratings** | **+1.834** | **−0.023** | **+1.431** | Headline |
| A3 market off (vs market-aware) | −1.323 | +0.018 | −1.044 | Market-aware harmed without snapshots |
| A4 single LGBM | +0.097 | −0.001 | +0.065 | Ensemble ≈ single LGBM here |
| A5 GT filter off | 0.000 | 0.000 | 0.000 | Silent no-op (no GT flags) |

MLflow run IDs: market `1fc0bf54…`, A1 `db30eda0…`, A2 `745e628d…`, A3
`c359b561…`, A4 `2ddf9ce4…`, A5 `7886b661…`. Fundamental first archive
`09ca146b…`; resume path left `mlflow_run_id=null` on re-score.

Artifacts: `docs/notes/_artifacts/task23/*.html`, `task23_memo.json`.

---

## Weekly error curve (fundamental, headline)

| Week | n | MAE |
|---:|---:|---:|
| 1 | 812 | 12.62 |
| 2 | 484 | 18.46 |
| 3 | 421 | 17.68 |
| 4 | 384 | **15.01** |
| 5 | 347 | 13.79 |
| 6 | 307 | 12.44 |
| 7 | 321 | 13.27 |
| 8 | 342 | 13.61 |
| 9 | 320 | 13.37 |
| 10 | 335 | **13.65** |
| 11–13 | … | ~13.1–13.7 |
| 14–16 | small n | noisier |

Week 10 < Week 4 holds. Week 1 looks “good” partly because early-season
margins and rating priors interact; Weeks 2–3 are the worst. Not a claim that
Week-1 forecasting is solved.

---

## Bet layer (§7.2 item 7)

- Bet at stored as-of line, never close; −110/−110; fractional Kelly + §12 filters.
- **As-of lines for 2021+ are null** (no snapshots). Bets that fired (n=64)
  are almost entirely 2019 CFBD rows where open was missing and the resolver
  used **close for both as-of and close** → number CLV and price CLV are **0
  by construction**.
- Mean CLV = 0.0 [0.0, 0.0] (n=64). Flat-stake ROI ≈ −10.5% [−30%, +9%].
- **Not** a §1.6 primary verdict. Snapshot-backed CLV remains unmeasured until
  Task 5B spend is approved (and ceiling raised or seasons batched).

---

## §1.6 success criteria — explicit miss list

| Criterion | Result |
|---|---|
| Mean CLV > 0, 95% CI excludes 0, n≥300 | **MISS** — n=64, CLV≡0, not snapshot CLV |
| Fundamental ATS ≥ 51.5% | **MISS** — 49.4% [47.4%, 51.8%] on 2019 only |
| Fundamental OU ≥ 51.5% | **PASS** on 2019 slice — 52.8% (narrow; not multi-season) |
| Brier/log-loss ≤ market baseline | **MISS** — ATS log-loss 1.05 vs 0.69 |
| Calibration slope ∈ [0.9, 1.1] | **MISS** — slope ≈ 0 |
| Zero leakage / process | Not re-litigated here; see 22B-FIX |

No hyperparameters were tuned against these numbers.

---

## What did not work

- Market-aware stack without snapshots is noise, not edge.
- Closing-line Normal CRPS beats the model by ~1.7 CRPS on margin.
- ATS below coin-flip after vig; OU barely clears 51.5% on one season of lines.
- Probabilities from fixed σ are badly calibrated (slope ≈ 0).
- A1/A5 ablations did not stress the intended subsystems (missing prior inputs /
  missing GT flags).
- Real CLV apparatus cannot run until historical odds are paid for.

---

## Ambiguities / smallest choices

1. Post-hoc σ=14 / 13.5 for CRPS and bet probs (production path has no σ heads).
2. CLV column = model cover-prob shift asof→close when prices are −110/−110
   (price-space CLV is identically 0); with asof≡close this is still 0.
3. Skipped returning/roster ingest rather than patching Task 3/5 schemas.
4. Did not `--force` Odds historical (54k > 16k ceiling; Task 5B required
   explicit go-ahead).

---

## Reproduction

```text
uv run ncaa-quant backtest plan --config task23_run_set
uv run ncaa-quant backtest run --config wiring_proof_2023 --stack fundamental
# Full run-set members: ncaa-quant backtest run --config <single-run name>
# HTML + memo: docs/notes/_artifacts/task23/
# 23-FIX artifacts: docs/notes/_artifacts/task23_fix/
```

---

## AMENDMENT — Task 23-FIX (2026-08-05)

Task 23 is **not closed**. Numbers above the amendment line are the original memo and
remain in the record as broken. They came from pipelines that did not assert
internal possibility. Superseding numbers below are from the repaired path;
where a metric cannot be computed honestly it is **NOT COMPUTED** / **NOT RUN**.

### Why the original numbers were wrong

1. **§2.3 / §2.6 joint distribution was disconnected.**
   `ProductionEnsemblePredictor.predict` emitted only `pred_margin` /
   `pred_total` ([production_stack.py](../src/ncaa_quant/evaluation/production_stack.py)
   pre-fix). Absent from the predict path (zero evaluation imports):
   - `models/heads/sigma.py` (`LightGBMSigmaHead`)
   - `models/ensemble.ensemble_sigma` (law of total variance)
   - `distribution/bivariate.py` / `estimate_rho` (ρ **not estimated**)
   - `distribution/key_numbers.py`
   - `distribution/simulate.py` 100k-draw MC
   - 50 epistemic Stage-1 posterior draws
   - `models/calibrate.py` / `models/conformal.py`
2. **Hardcoded σ.** Deleted `scripts/_task23_backtest.py` applied `σ_m=14`,
   `σ_t=13.5` and Normal CDFs. CRPS 10.28 and every memo probability measured
   a typed constant, not model uncertainty.
3. **CLV ≡ 0 by construction.** For 2019 CFBD rows with missing open, as-of and
   close both resolved to the close row (`walkforward.resolve_lines_for_games`).
   No 2019 Odds snapshots exist. Mean 0.0 [0.0, 0.0] is not a finding.
4. **Roster family missing.** Schema `ge=0` rejected CFBD negatives on
   `athlete_id` / returning fields → returning/roster/talent/portal priors
   skipped → every headline metric came from a system missing that family.
5. **A1 / A5 inert.** Fitted priors collapsed without roster inputs; staged
   plays carry no effective `garbage_time` flags (`n_on == n_off`).

### Repairs shipped

| Item | Change |
|---|---|
| P0-1 | Wired σ heads, LoTV ensemble σ, ρ, key-number kernel, MC probs, epistemic mixture, isotonic/Platt (OOF-only per retrain), CQR into `production_stack`; additive `PREDICTION_COLUMNS` pass-through in `walkforward` (null-with-indicator, never σ=14); degeneracy guard |
| P0-2 | `ClvError` / `assert_distinct_line_sources` when bet-time and close share a source row; CLV struck |
| P0-3 | Deleted `scripts/_task23_backtest.py`; reproduction via `ncaa-quant backtest` / `run_backtest` only |
| P1-4 | Runtime A1/A5 precondition asserts; 22B tests assert precondition failure on inert inputs |
| P1-5 | Schema: opaque `athlete_id`; returning pcts allow negatives; rematerialized `data/features/roster_task23_fix.parquet` (portal_net negatives present; returning nulls preserved) |
| P2-6 | `naive_proportion_ci` beside block-bootstrap; CI level = 95% (`α=0.05`); blocks = week-label groups, variable length |
| P2-7 | A2 components reported per basis below |
| P2-8 | 5B cost ladder below (no purchase) |

### Calibration call path (after fix)

`WalkForwardHarness._retrain` → `ProductionEnsemblePredictor.fit` (time-ordered
blocked OOF → σ heads → `estimate_rho` → key-number kernel → `fit_cqr` →
`fit_market_calibrator` on OOF raw probs only) → `predict` → MC raw probs →
`CalibrationBundle.transform` → harness copies distributional columns.

### Calibration slope / intercept (OOF, last retrain on 2023 smoke)

| Market | kind | n | slope_before | intercept_before | slope_after | intercept_after |
|---|---|---:|---:|---:|---:|---:|
| ml | isotonic | 509 | 0.745 | 0.134 | **1.001** | ≈0 |
| ats_close | isotonic | 507 | 0.411 | −0.137 | **0.997** | ≈0 |
| ou_close | isotonic | 507 | 0.064 | 0.007 | **1.000** | ≈0 |

Plots (held-out 2023 smoke): `docs/notes/_artifacts/task23_fix/reliability_2023.png`,
`docs/notes/_artifacts/task23_fix/pit_2023.png`.

### Superseding smoke metrics (2023 fundamental, CLI-equivalent `run_backtest`)

Smoke used `n_mc_draws=800`, `n_epistemic_draws=3` for wall-clock; production
default remains `DEFAULT_N_DRAWS=100_000` / 50 epistemic. **Original memo
numbers are unverified** until the full run-set is reproduced that way.

| Metric | Value | Notes |
|---|---|---|
| n_predictions | 910 | 2023 only |
| MAE margin | 16.60 | |
| CRPS margin (model) | 12.91 | heteroskedastic σ (910 unique values) |
| CRPS vs market | NOT COMPUTED | `line_source_close=null` for all 2023 rows (no Odds snapshots; CFBD excluded ≥2021) |
| ATS / OU vs close | NOT COMPUTED | same — zero finite `spread_close` |
| Log-loss ML (model / market@0.5) | 1.088 / 0.693 | **still loses to market after calibration** |
| Pooled Cox slope ML | 0.114 | OOF fit reaches ~1; pooled walk-forward emit still poorly calibrated |
| CLV | **NOT COMPUTED** | no distinct bet-time price (2019 open missing → asof≡close; ≥2021 null without snapshots) |
| Determinism | **PASS** | same config+seed → byte-identical prediction table |
| run_id / ablation_id / manifest | present | on every prediction row + `manifest.json` |

**Honest conclusion from the measurable slice:** after wiring real σ and
OOF isotonic, the system still loses to a flat 0.5 ML baseline on log-loss and
has no snapshot-backed ATS/CLV. **The system is NOT CURRENTLY FIT TO BET.**

### A1 / A5

| Ablation | Status | Missing input |
|---|---|---|
| A1 | Re-run after roster rematerialize when priors_frame is non-degenerate; else **NOT RUN** | fitted priors from returning/talent (schema now accepts negatives; priors must be rebuilt into the stack) |
| A5 | **NOT RUN** | garbage-time flags: staged plays `n_on == n_off` (159011) — filter cannot change the play set |

### A2 components by basis (structure; full CLI re-run pending)

| Component | Needs lines? | Seasons | n | Note |
|---|---|---|---|---|
| MAE margin | no | all headline seasons in run | all finite preds | continuous |
| CRPS margin | no | same | same | continuous |
| ATS accuracy | yes | line seasons only (2019 CFBD today; 2021+ when snapshots exist) | n with finite `spread_close` | never pool with null-line seasons |

Original A2 Δ (MAE +1.83, ATS −2.3 pp, CRPS +1.43) mixed bases; treat as
**unverified** until reproduced through `ncaa-quant backtest` with the wired
distributional path.

### CI construction (P2-6)

- Confidence level: **95%** (`DEFAULT_ALPHA = 0.05`)
- Block bootstrap: resample **whole week-label groups**; block length =
  variable (# games or bets in that week)
- Naive Wald interval attached as `*_naive` beside bootstrap for headline
  proportions. At n=865, p=0.494 the naive 95% interval is ≈ [46.1%, 52.7%];
  any bootstrap narrower than that is a bug — report both side by side when
  ATS is measurable.

### 5B scope / cost ladder (estimate only — no purchase)

Budget: plan 20,000 credits/mo; ceiling 16,000; notes record 24 used →
remaining under ceiling ≈ **15,976**.

| Scope | Requests (approx) | Credits | Fits remaining? |
|---|---:|---:|---|
| 2021–2025, all books, all DPs (baseline) | 1803 | **54,090** | **No** |
| One season (2024), all DPs | 385 | 11,550 | Yes under ceiling |
| One season, tuesday_0600_et only (~1 DP/week) | ~16 | ~480 | Yes |
| Two seasons, tuesday only | ~32 | ~960 | Yes |
| 2024–2025, tuesday only | ~32 | ~960 | Yes |
| 2024–2025, all DPs | 782 | 23,460 | No (over ceiling) |
| One season, slot_close only (1 DP/game) | ~369 | ~11,070 | Yes under ceiling |

Credits/call = 30 (`10 × markets × regions`); “one book” does not reduce that
meter under the current estimator. A reduced scope that validates the bet layer
on one season is worth more than an unvalidated bet layer.

### Reproduction (23-FIX)

```text
uv run ncaa-quant backtest run --config wiring_proof_2023 --force
# Prediction table must carry sigma_m (non-constant), p_*_raw / p_*, run_id, ablation_id
# Manifest at data/backtests/<run_id>/<ablation_id>/manifest.json
# Degenerate CLV raises ClvError / MetricsError — never report 0.0 [0.0, 0.0]
```

---

## AMENDMENT — Task 23-FIX-DIAG (2026-08-05)

Diagnosis only on the staged 2023 smoke (`wiring_proof_2023`, n=910). No
hyperparameter, threshold, prior, or clipping changes. Numbers supersede the
FIX amendment's "loses to market@0.5" claim; they are still **instrumentation on
one season**, not a model-quality verdict.

### D-1 — Market baseline resolution path

**Trace (archived prediction frame):**

1. `backtest_runner` fills `p_mkt_ats_home = p_mkt_ou_over = 0.5` (fair at
   −110/−110 — correct for ATS/OU).
2. `p_mkt_ml_home = 1/(1+exp(spread_close/10))`. For 2023,
   `spread_close` is all-null (`line_source_close=null` — no Odds snapshots;
   CFBD excluded from the §7.2 item 8 bet-time ladder) → **all-null ML baseline**.
3. The FIX amendment's `0.693` was `ln(2)` from a coin-flip comparison, not a
   de-vigged moneyline. Claiming the calibrated model "loses to the market" on
   that number was **unsupported**.

**Fix (metrics.py):** `resolve_market_baselines` now:
- ML from proportional de-vig of `home_ml`/`away_ml` when present; else finite
  non-constant `p_mkt_ml_home`; else **`NOT_COMPUTED`** (market=`NaN`).
- Constant-0.5 ML is struck as strawman (same treatment class as degenerate CLV).
- ATS/OU fair-0.5 at −110/−110 remains valid.

On the archived smoke frame (no American odds columns): ML market =
**NOT COMPUTED**. Model log-loss remains **1.088**; market side is `NaN`.

**Diagnostic join** (CFBD `lines_historical` close ML → frame; not wired into
walkforward — that would need runner edits outside this task's sanctioned set):

| Slice | n | Model LL | Market LL (de-vigged CFBD close ML) |
|---|---:|---:|---:|
| Smoke frame as archived | 910 | 1.088 | **NOT COMPUTED** |
| Overlap with both-side CFBD ML | 787 | 1.158 | **0.536** |

Real CFBD close ML lands near the expected 0.55 band (home win rate on overlap
≈ 57.8%). Against that yardstick the stack loses badly — but that comparison is
a diagnostic join, not the walkforward emit. Until Odds snapshots (or an
explicit, design-approved CFBD-close evaluation attach) put prices on the
prediction frame, report ML market as **NOT COMPUTED**.

### D-2 — Sigma diagnostics (underdispersion)

Smoke MAE margin = **16.60** (not the original memo's Week-10 13.65) with
ML log-loss **1.088**. Mu is mediocre; sigma is the catastrophic piece.

| Diagnostic | Value | Reading |
|---|---:|---|
| (a) Mean predicted σ_m | **11.35** | |
| (a) Realized residual SD | **20.37** | |
| (a) Ratio mean(σ)/resid_SD | **0.557** | underdispersed (~half the needed width) |
| (b) PIT outer/middle density | **3.55** | **U-shaped** (bins: 1.37,…,3.16 at right tail) |
| (c) Gaussian coverage @50/80/95% | **31.0% / 54.6% / 68.2%** | well under nominal |
| (c) CQR @80% (emit) | **88.5%** cover, width 54.9, n=547 (363 missing) | wired; only one nominal on emit; not fixing the σ head |
| (d) Pooled LL / without top-10 | **1.088 / 0.947** | top-10 matter but are not the whole story |

**Clipping bounds in use:** calibrate / isotonic `1e-6 … 1−1e-6`; metrics
`log_loss` clips `1e-15 … 1−1e-15`. Emitted `p_ml_home` hits the calibrate floor
(`min=1e-6`, `max=1−1e-6`).

**Top 10 log-loss contributors** (all home wins where calibrated p hit `1e-6`;
raw p was 0.05–0.28 — calibration drove them to the floor):

| game_id | week | margin pred | σ | p_raw | p_cal | LL |
|---:|---:|---:|---:|---:|---:|---:|
| 401539475 | 14 | −13.6 | 12.0 | 0.083 | 1e-6 | 13.82 |
| 401531924 | 13 | −13.1 | 12.2 | 0.130 | 1e-6 | 13.82 |
| 401524031 | 7 | −8.7 | 12.7 | 0.259 | 1e-6 | 13.82 |
| 401525864 | 7 | −9.4 | 15.1 | 0.275 | 1e-6 | 13.82 |
| 401524034 | 7 | −13.6 | 14.3 | 0.170 | 1e-6 | 13.82 |
| 401525525 | 7 | −17.3 | 12.4 | 0.088 | 1e-6 | 13.82 |
| 401531928 | 13 | −12.2 | 7.9 | 0.053 | 1e-6 | 13.82 |
| 401532415 | 6 | −11.7 | 12.6 | 0.194 | 1e-6 | 13.82 |
| 401525439 | 6 | −9.7 | 11.5 | 0.221 | 1e-6 | 13.82 |
| 401525857 | 6 | −12.4 | 10.1 | 0.095 | 1e-6 | 13.82 |

**Plain finding: the sigma path is underdispersed.** Mean σ ≈ 11 vs residual SD ≈
20; PIT is U-shaped; Gaussian intervals undercover at every nominal. That is a
concrete, fixable defect (σ head / LoTV / epistemic mixture scale) — far more
actionable than "the model underperforms." CQR at 80% on the rows that carry it
is not a substitute for fixing σ. Separately, OOF isotonic on thin one-season
smoke is shoving mid-range raw probs to the 1e-6 clip against the actual winner.

Plots: `docs/notes/_artifacts/task23_fix/pit_2023_diag.png`,
`reliability_2023_diag.png`. JSON: `diag_23_fix.json`.

### D-3 — Elo on identical games

Elo (Task 13 defaults, chronological pass over staged 2014–2025 games) scored on
the intersection with the smoke prediction `game_id`s:

| System | n | ML log-loss | MAE margin |
|---|---:|---:|---:|
| Elo baseline | 792 | **0.557** | **13.54** |
| Full stack (smoke) | 792 | **1.153** | **14.83** |

Elo near 0.56 where the stack is at 1.15 → **the problem is in the stack, not
the harness or labels.** Highest-information diagnostic available; run first
when anything blocks.

### D-4 — A5 garbage-time redefinition (PROPOSAL ONLY — not implemented)

**Blocked today:** staged plays have `wp` all-null, and also lack `clock`,
`offense_score`, `defense_score`, `score_margin`. The existing Connelly fallback
in `features/epa.py` cannot fire without score+period. A5 remains NOT RUN.

**Proposed primary rule** (replaces WP >0.98 / <0.02 as the production GT
definition for seasons where CFBD WP is absent — empirically all staged seasons
probed so far). Apply before efficiency aggregates; labels never filtered.

Garbage-time when **either**:

1. **Score-margin-by-period (Connelly / FO published thresholds — lock these
   before any A5 delta is measured):**
   - Period 1: `|score_margin| > 28`
   - Period 2: `|score_margin| > 24`
   - Period 3: `|score_margin| > 21`
   - Period 4 and OT (period ≥ 4): `|score_margin| > 16`
2. **Clock-aware late blowout (additional, only when clock seconds remaining
   are present):** period ∈ {2, 4} (end of half) **and**
   `|score_margin| > 24` **and** `clock_seconds_remaining ≤ 120`; **or**
   period = 4 **and** `|score_margin| > 21` **and**
   `clock_seconds_remaining ≤ 300`.

**Score_margin definition:** offense_score − defense_score at the snap (same
sign convention as current `connelly_garbage_time`). Period from staged
`period` (1–4; ≥5 uses Q4 threshold).

**Seasons:** all seasons in the production window (2014–2025) until/unless WP
is verified non-null at source for a season — then WP-threshold may resume as
primary with this rule as fallback (current code shape). **Do not tune
thresholds against A5 ablation deltas.**

**Prerequisite before A5 can run:** normalize `offense_score`, `defense_score`
(or equivalent), and `clock` onto staged plays (ingest/schema — out of this
diag's sanctioned edits). Period alone is insufficient.

### D-5 — Corrected 5B ladder + ceiling

**One historical call returns ALL events at a timestamp**
(`GET /v4/historical/sports/americanfootball_ncaaf/odds`). Meter = 30 credits =
`10 × markets × regions`. Not per-event.

The 54,090 baseline is **tuesday_0600_et + slot_close** (slot_close ≈ one
request per unique kickoff−5min), not every 5-minute snapshot interval. For the
bet layer's Tuesday decision point only:

| Scope | Requests | Credits @30 | Fits 20k ceiling? |
|---|---:|---:|---|
| 2021–2025, tuesday+slot_close (old baseline) | 1803 | **54,090** | No |
| 2021–2025, **tuesday_0600_et only** | 77 | **2,310** | **Yes** |
| 2024, tuesday only | 16 | 480 | Yes |
| 2024–2025, tuesday only | 32 | 960 | Yes |
| 2024, slot_close only | 369 | 11,070 | Yes |
| 2024–2025, slot_close only | 750 | 22,500 | No |
| 2024, tuesday+slot_close | 385 | 11,550 | Yes |

Rule-of-thumb `5 × 15 × 1 × 30 = 2,250` matches the tuesday-only rung (~2,310
with real week counts). **No purchase made.**

**Ceiling:** `odds_historical_credit_ceiling` raised **16,000 → 20,000** in
`configs/data.yaml` and `config.py` default (equals DESIGN §3.2 monthly plan).
Stale "24 used" remains a notes artifact; live probe recorded 27 then 57 after
one historical call (`docs/notes/data-check.md`).

### Honest conclusion (instrumentation, not evidence)

On 2023 smoke: ML market baseline is **NOT COMPUTED** on the archived frame;
against a diagnostic CFBD-close ML join the stack loses (1.16 vs 0.54). The
sigma path is **underdispersed** (ratio 0.56, U-shaped PIT, intervals
undercover). Elo at 0.56 LL on the same games shows the harness/labels are
fine and the defect is in the stack. Still **not fit to bet**; still not a
multi-season quality claim.

### Reproduction (diag)

```text
uv run python scripts/_task23_fix_diag.py
# Artifacts: docs/notes/_artifacts/task23_fix/diag_23_fix.json
#            pit_2023_diag.png  reliability_2023_diag.png
```

---

## AMENDMENT — Task 23-RERUN (2026-08-10)

**Plain statement:** ALL prior Task 23 numbers in this file — the original
results memo, the 23-FIX / 23-FIX-DIAG superseding smoke figures, wiring-proof
deltas, and any ablation deltas measured while garbage-time was inert and/or
roster priors were missing — came from a **GT-inert system missing roster
priors** (and, for market / CLV slices, often `snapshots=None`). They are
**superseded in full**. They must not be cited as evidence of model skill,
continual-learning gain, GT value, or bet-layer edge.

The intended replacement measurement is the eight-run set in
`configs/ablations/task23_run_set.yaml` via `ncaa-quant backtest`, after GT-FIX
and 5B. That rerun **did not execute** in the 23-RERUN session: Step 0 stopped
on an undefined expected-possessions production resolve path and on the CLI
still passing `snapshots=None` despite staged 2021–2024 odds. See
`docs/notes/23-rerun.md`. No new headline numbers are written here.
