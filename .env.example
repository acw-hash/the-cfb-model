# API keys (workstation only — never in Vercel / git)
CFBD_API_KEY=
ODDS_API_KEY=
NTFY_AUTH_TOKEN=
TELEGRAM_BOT_TOKEN=

# R2 write credentials (workstation only — never in Vercel)
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=

# Ridge private-preview publish (workstation)
# Flip export ON only for the preview workstation; default in code remains false.
# NCAA_QUANT_WEBAPP__EXPORT_ENABLED=true
# NCAA_QUANT_WEBAPP__R2_BUCKET=ridge-artifacts-preview
# NCAA_QUANT_WEBAPP__R2_ENDPOINT_URL=https://<accountid>.r2.cloudflarestorage.com
# NCAA_QUANT_WEBAPP__REVALIDATE_URL=https://<protected-preview>.vercel.app/api/revalidate
# Shared with Vercel server env (not the R2 write key):
# WEBAPP_REVALIDATE_SECRET=
